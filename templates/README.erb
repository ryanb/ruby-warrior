Level <%= level.number %>

<%= level.description %>

Tip: <%= level.tip %>

<%= level.floor.to_map -%>

  > = Stairs
<%- for unit in level.floor.unique_units -%>
  <%= unit.to_map %> = <%= unit.name %> (<%= unit.max_health %> HP)
<%- end -%>
